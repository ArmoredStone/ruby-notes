Controller is a file that defines how Ruby(basically [[Model]]) data is presented in [[View]].
[[Route]] is referring to a controller method when been accessed.

creation
	`bin/rails generate controller Articles index`